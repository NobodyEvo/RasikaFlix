{
  "name": "rasikaflix",
  "version": "1.0.0",
  "description": "A Netflix-inspired birthday website — RasikaFlix ❤️",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "canvas-confetti": "^1.9.3"
  }
}
